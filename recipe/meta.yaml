
{% set data = load_setup_py_data() %}

package:
  name: emboa
  
  version: {{ data['version'] }}
  
source:
  path: ../


build:
  noarch: python
  number: 0
  
  script: "{{ PYTHON }} -m pip install --no-deps --ignore-installed ."
  
requirements:
  host:
    - python
  run:
    - python >=3.6
    # dependencies are defined in setup.py
    
    {% for dep in data['install_requires'] %}
    - {{ dep.lower() }}
    {% endfor %}
    
test:
  imports:
    - emboa

about:
  home: https://github.com/DerThorsten/emboa
  license: MIT
  license_family: MIT
  
  license_file: ../LICENSE.txt
  summary: 'emboa emscripten+boa'
  description: 'emboa emscripten+boa'
  documentation_url: https://readthedocs.org/projects/emboa/
  dev_url: https://github.com/DerThorsten/emboa

extra:
  recipe-maintainers:
    - Thorsten Beier


